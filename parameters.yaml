avatar:
  $ref: parameters/avatar.yaml

userId:
  $ref: parameters/userId.yaml

streamId:
  $ref: parameters/streamId.yaml

replayId:
  $ref: parameters/replayId.yaml

organizationId:
  $ref: parameters/organizationId.yaml

user:
  $ref: parameters/user.yaml

stream:
  $ref: parameters/stream.yaml

organization:
  $ref: parameters/organization.yaml

limit:
  $ref: parameters/limit.yaml

country:
  $ref: parameters/country.yaml

start:
  $ref: parameters/start.yaml

tag:
  $ref: parameters/tag.yaml

tagId:
  $ref: parameters/tagId.yaml

login:
  $ref: parameters/login.yaml

jwt:
  $ref: parameters/jwt.yaml
