description: >
  This will contain your organizations, this result will be split.
  You must recall this route by incrementing start of x times limit.
  x is the number of page that you want. If you ask for a out of range value,
  you will have a 404 error.

schema:
  type: object

  properties:
    organization_list:
      type: array

      items:
        $ref: ../definitions.yaml#/organization

    organization_total:
      type: integer
