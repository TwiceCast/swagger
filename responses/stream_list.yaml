description: >
  This will contain your streams, this result will be split.
  You must recall this route by incrementing start of x times limit.
  x is the number of page that you want.
  If you ask for an out of range value, you will have a 404 error.

schema:
  type: object

  properties:
    stream_list:
      type: array

      items:
        $ref: ../definitions.yaml#/stream

    stream_total:
      type: integer
