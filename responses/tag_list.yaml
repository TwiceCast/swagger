description: >
  This will contain your tags, this result will be split.
  You must recall this route by incrementing start of x times limit.
  x is the number of page that you want.
  If you ask for an out of range value, you will have a 404 error.

schema:
  type: object

  properties:
    tag_list:
      type: array

      items:
        $ref: ../definitions.yaml#/tag

    tag_total:
      type: integer
