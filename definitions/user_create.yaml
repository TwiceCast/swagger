properties:
  name:
    type: string

    description: The public name of the user.

  email:
    type: string

    format: email

    description: The API use the email as login.

  password:
    type: string

    format: password

    description: The password will be hash by the server.

  language:
    $ref: ../definitions.yaml#/language
