get:
  tags:
    - Stream

  description: get info of a stream

  responses:
    200:
      $ref: ../responses.yaml#/stream

parameters:
  - $ref: ../parameters.yaml#/stream_id

  - $ref: ../parameters.yaml#/jwt
