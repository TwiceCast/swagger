get:
  tags:
    - Tag

  description: Return one tag

  responses:
    200:
      $ref: ../responses.yaml#/tag

put:
  tags:
    - Tag

  description: Replace one tag

  responses:
    200:
      $ref: ../responses.yaml#/tag

patch:
  tags:
    - Tag

  description: Patch one tag

  responses:
    200:
      $ref: ../responses.yaml#/tag

delete:
  tags:
    - Tag

  description: Delete a tag

  responses:
    204:
      description: The tag has been deleted.

parameters:
  - $ref: ../parameters.yaml#/tag_id

  - $ref: ../parameters.yaml#/jwt
