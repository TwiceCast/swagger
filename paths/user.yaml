get:
  tags:
    - User
  description: Return you
  responses:
    200:
      description: your user
      schema:
        type: array
        items:
          $ref: ../definitions.yaml#/user
    404:
      description: not found
      schema:
        $ref: ../definitions.yaml#/error
post:
  tags:
    - User
  description: Create a user
  responses:
    200:
      description: the new user create
      schema:
        $ref: ../definitions.yaml#/user
  parameters:
    - $ref: ../parameters.yaml#/user
parameters:
  - $ref: ../parameters.yaml#/signature
