/:
  options:
    tags:
      - Options
    description: Return description of API
    responses:
      200:
       $ref: responses.yaml#/option

/user:
  $ref: paths/user.yaml

/users:
  $ref: paths/users.yaml

/user/{userId}:
  $ref: paths/user_id.yaml

/user/{userId}/organizations:
  $ref: paths/user_id_organizations.yaml

/user/{userId}/avatar:
  $ref: paths/user_id_avatar.yaml

/user/{userId}/friends:
  $ref: paths/user_id_friends.yaml

/user/{userId}/blocks:
  $ref: paths/user_id_blocks.yaml

/user/{userId}/follows:
  $ref: paths/user_id_follows.yaml

/user/{userId}/subscribes:
  $ref: paths/user_id_subscribes.yaml

/streams:
  $ref: paths/streams.yaml

/stream/{streamId}:
  $ref: paths/stream_id.yaml

/stream/{streamId}/replays:
  $ref: paths/stream_id_replays.yaml

/stream/{streamId}/blocks:
  $ref: paths/stream_id_blocks.yaml

/stream/{streamId}/followers:
  $ref: paths/stream_id_followers.yaml

/stream/{streamId}/subscribers:
  $ref: paths/stream_id_subscribers.yaml

/stream/{streamId}/members:
  $ref: paths/stream_id_members.yaml

/stream/{streamId}/chat:
  $ref: paths/stream_id_chat.yaml

/stream/{streamId}/event:
  $ref: paths/stream_id_event.yaml

/stream/{streamId}/project:
  $ref: paths/stream_id_project.yaml

/stream/{streamId}/tags:
  $ref: paths/stream_id_tags.yaml

/organizations:
  $ref: paths/organizations.yaml

/organizations/{organizationId}:
  $ref: paths/organizations_id.yaml

/organizations/{organizationId}/streams:
  $ref: paths/organizations_id_streams.yaml

/organizations/{organizationId}/members:
  $ref: paths/organizations_id_members.yaml

/replays:
  $ref: paths/replays.yaml

/replays/{replayId}:
  $ref: paths/replays_id.yaml

/replays/{replayId}/chat:
  $ref: paths/replays_id_chat.yaml

/replays/{replayId}/event:
  $ref: paths/replays_id_event.yaml

/replays/{replayId}/project:
  $ref: paths/replays_id_project.yaml

/tags:
  $ref: paths/tags.yaml

/tags/{tagId}:
  $ref: paths/tags_id.yaml
