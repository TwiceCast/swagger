avatar:
  $ref: responses/avatar.yaml

stream:
  $ref: responses/stream.yaml

user:
  $ref: responses/user.yaml

user_list:
  $ref: responses/user_list.yaml

user_not_found:
  $ref: responses/user_not_found.yaml

stream_list:
  $ref: responses/stream_list.yaml

stream_not_found:
  $ref: responses/stream_not_found.yaml

organization:
  $ref: responses/organization.yaml

organization_list:
  $ref: responses/organization_list.yaml

option:
  $ref: responses/option.yaml

tag:
  $ref: responses/tag.yaml

tag_list:
  $ref: responses/tag_list.yaml

tag_not_found:
  $ref: responses/tag_not_found.yaml

bad_request:
  $ref: responses/bad_request.yaml

unauthorized:
  $ref: responses/unauthorized.yaml

forbidden:
  $ref: responses/forbidden.yaml

not_found:
  $ref: responses/not_found.yaml

unsupported_media_type:
  $ref: responses/unsupported_media_type.yaml
