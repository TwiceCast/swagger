stream:
  $ref: definitions/stream.yaml

organization:
  $ref: definitions/organization.yaml

user:
  $ref: definitions/user.yaml


server:
  $ref: definitions/server.yaml

country:
  $ref: definitions/country.yaml

language:
  $ref: definitions/language.yaml

rank:
  $ref: definitions/rank.yaml

error:
  $ref: definitions/error.yaml

tag:
  $ref: definitions/tag.yaml

avatar:
  $ref: definitions/avatar.yaml


register:
  $ref: definitions/register.yaml

login:
  $ref: definitions/login.yaml

jwt:
  $ref: definitions/jwt.yaml


chat:
  $ref: definitions/chat.yaml

event:
  $ref: definitions/event.yaml

repository:
  $ref: definitions/repository.yaml
