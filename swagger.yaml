swagger: "2.0"

info:
  title: TwiceCast API
  description: TwiceCast api specification
  version: 0.1.0
  contact:
    name: TwiceCast
    email: twicecast_2018@labeip.epitech.eu
  license:
    name: MIT
    url: https://opensource.org/licenses/mit-license.php

host: ns3005990.ip-37-187-99.eu

schemes:
  - https
produces:
  - application/json
consumes:
  - application/json

paths:
  $ref: paths.yaml
parameters:
  $ref: parameters.yaml
responses:
  $ref: responses.yaml
definitions:
  $ref: definitions.yaml
